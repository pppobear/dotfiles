#!/usr/bin/env zsh
# ~/.config/zsh/.zshenv
# This file is sourced on all invocations of the shell
# Generated by chezmoi - DO NOT EDIT MANUALLY

# Set ZDOTDIR if not already set
export ZDOTDIR="${ZDOTDIR:-$HOME/.config/zsh}"

# Source core environment variables
source "$ZDOTDIR/core/environment.zsh"

# Resolve SSH agent early so non-interactive shells use the preferred socket.
[ -f "$ZDOTDIR/core/ssh-agent.zsh" ] && source "$ZDOTDIR/core/ssh-agent.zsh"
